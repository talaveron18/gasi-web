name: Internal Prototype CI

on:
  push:
    branches:
      - 'automation/web-internal-clinical-*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: yarn install --non-interactive
      - name: Build
        env:
          CI: 'true'
        run: yarn build
      - name: Tests
        env:
          CI: 'true'
        run: yarn test --watchAll=false --passWithNoTests

  backend-prototype:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install minimal test dependencies
        run: pip install fastapi starlette pydantic python-dotenv motor pytest httpx
      - name: Synthetic API tests
        env:
          ENABLE_INTERNAL_SYNTHETIC_PROTOTYPE: 'true'
        run: pytest -q tests/test_internal_prototype.py --junitxml=synthetic-api-junit.xml
      - name: Preserve synthetic API diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: synthetic-api-test-results
          path: backend/synthetic-api-junit.xml
          if-no-files-found: warn
