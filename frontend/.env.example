# Public frontend configuration (example only; do not commit real credentials)
REACT_APP_BACKEND_URL=
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false

# Synthetic internal-zone prototype only. Keep false unless the synthetic backend
# router is intentionally enabled in a non-production test environment.
REACT_APP_INTERNAL_SYNTHETIC_API=false

# Server-side Netlify function secrets: configure in the deployment environment.
MAILERSEND_API_KEY=
CALLMEBOT_PHONE=
CALLMEBOT_API_KEY=
