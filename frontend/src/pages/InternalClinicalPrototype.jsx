import React, { useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { AlertTriangle, CheckCircle2, Clock3, HeartPulse, History, LockKeyhole, LogOut, PhoneCall, Plus, Send, ShieldCheck, Stethoscope, UserCog } from 'lucide-react';
import { useInternalPrototypeAuth } from '@/contexts/InternalPrototypeAuthContext';

const ROLES = {
  nurse: { label: 'Enfermería', icon: HeartPulse },
  physician: { label: 'Facultativo', icon: Stethoscope },
  admin: { label: 'Administración / Coordinación', icon: UserCog },
};

const LEVELS = {
  1: { label: 'Nivel 1 · Urgencia', description: 'Registro + llamada telefónica directa. La plataforma no sustituye la llamada.' },
  2: { label: 'Nivel 2 · Consulta médica no aguda', description: 'Canal escrito que requiere criterio facultativo.' },
  3: { label: 'Nivel 3 · Gestión', description: 'Gestión o documentación. Funciones clínicas condicionadas permanecen bloqueadas.' },
};

const initialCases = [
  {
    id: 'DEMO-2026-001', patientRef: 'PACIENTE-DEMO-A', center: 'Centro ficticio Madrid 01', level: 2, status: 'ABIERTO',
    summary: 'Paciente sintético con mareo leve tras actividad. Sin datos reales.', createdBy: 'Enfermera Demo 01', createdById: 'USR-DEMO-NURSE-01', createdAt: '2026-09-12 12:10', responses: [],
    audit: [{ at: '2026-09-12 12:10', actor: 'Enfermera Demo 01', actorId: 'USR-DEMO-NURSE-01', role: 'Enfermería', action: 'Caso creado · Nivel 2' }], addenda: [], levelHistory: [],
  },
  {
    id: 'DEMO-2026-002', patientRef: 'PACIENTE-DEMO-B', center: 'Centro ficticio Madrid 01', level: 1, status: 'RESPONDIDO',
    summary: 'Escenario sintético de dolor torácico para probar contingencia y trazabilidad.', createdBy: 'Enfermera Demo 01', createdById: 'USR-DEMO-NURSE-01', createdAt: '2026-09-12 11:35',
    responses: [{ id: 'DEMO-2026-002-R1', text: 'Respuesta sintética: circuito urgente atendido por teléfono. No constituye indicación clínica real.', author: 'Dr. Demo 01', authorId: 'USR-DEMO-PHYS-01', createdAt: '2026-09-12 11:39', status: 'ENTREGADA', statusHistory: [{ at: '2026-09-12 11:39', from: 'BORRADOR', to: 'EMITIDA' }, { at: '2026-09-12 11:40', from: 'EMITIDA', to: 'ENTREGADA', evidence: 'acuse técnico sintético' }] }],
    respondedBy: 'Dr. Demo 01', respondedById: 'USR-DEMO-PHYS-01', respondedAt: '2026-09-12 11:39', acknowledgementRequired: true, followUpPending: false,
    audit: [{ at: '2026-09-12 11:35', actor: 'Enfermera Demo 01', actorId: 'USR-DEMO-NURSE-01', role: 'Enfermería', action: 'Caso creado · Nivel 1' }, { at: '2026-09-12 11:36', actor: 'Enfermera Demo 01', actorId: 'USR-DEMO-NURSE-01', role: 'Enfermería', action: 'Llamada telefónica directa registrada (simulación)' }, { at: '2026-09-12 11:39', actor: 'Dr. Demo 01', actorId: 'USR-DEMO-PHYS-01', role: 'Facultativo', action: 'Respuesta emitida' }], addenda: [], levelHistory: [],
  },
];

const statusBadge = (status) => ({ ABIERTO: 'bg-amber-100 text-amber-800 border-amber-200', RESPONDIDO: 'bg-blue-100 text-blue-800 border-blue-200', CERRADO: 'bg-emerald-100 text-emerald-800 border-emerald-200' }[status] || 'bg-slate-100 text-slate-700 border-slate-200');
const messageBadge = (status) => ({ EMITIDA: 'bg-slate-700 text-slate-100', ENTREGADA: 'bg-cyan-950 text-cyan-200', LEIDA: 'bg-emerald-950 text-emerald-200' }[status] || 'bg-slate-800 text-slate-300');
const now = () => new Date().toLocaleString('es-ES', { hour12: false });

export default function InternalClinicalPrototype() {
  const navigate = useNavigate();
  const { session, signOut } = useInternalPrototypeAuth();
  const role = session?.role;
  const [cases, setCases] = useState(initialCases);
  const [selectedId, setSelectedId] = useState(initialCases[0].id);
  const [showCreate, setShowCreate] = useState(false);
  const [responseText, setResponseText] = useState('');
  const [addendumText, setAddendumText] = useState('');
  const [draft, setDraft] = useState({ patientRef: 'PACIENTE-DEMO-C', center: 'Centro ficticio Madrid 01', level: 2, summary: '' });
  const selected = useMemo(() => cases.find((item) => item.id === selectedId) || cases[0], [cases, selectedId]);
  const visibleCases = useMemo(() => role === 'physician' ? cases.filter((item) => item.status !== 'CERRADO') : role === 'nurse' ? cases.filter((item) => session?.centers?.includes(item.center)) : cases, [cases, role, session]);
  const pendingCount = visibleCases.filter((item) => item.status !== 'CERRADO').length;
  const RoleIcon = ROLES[role]?.icon || LockKeyhole;
  if (!session) return null;

  const actor = () => ({ actor: session.displayName, actorId: session.id, role: session.roleLabel });
  const patchCase = (caseId, patch, auditAction) => { const timestamp = now(); setCases((current) => current.map((item) => item.id === caseId ? { ...item, ...patch, audit: auditAction ? [...item.audit, { at: timestamp, ...actor(), action: auditAction }] : item.audit } : item)); };
  const createCase = (event) => { event.preventDefault(); if (role !== 'nurse' || !draft.summary.trim()) return; const timestamp = now(); const id = `DEMO-${Date.now()}`; const level = Number(draft.level); const next = { id, patientRef: draft.patientRef.trim() || 'PACIENTE-DEMO', center: draft.center.trim() || session.centers[0], level, status: 'ABIERTO', summary: draft.summary.trim(), createdBy: session.displayName, createdById: session.id, createdAt: timestamp, responses: [], addenda: [], levelHistory: [], acknowledgementRequired: false, followUpPending: false, audit: [{ at: timestamp, ...actor(), action: `Caso creado · Nivel ${level}` }] }; setCases((current) => [next, ...current]); setSelectedId(id); setShowCreate(false); setDraft({ patientRef: 'PACIENTE-DEMO-C', center: session.centers[0], level: 2, summary: '' }); };
  const changeLevel = (nextLevel) => { if (role !== 'nurse' || !selected || selected.status === 'CERRADO' || selected.level === nextLevel) return; const timestamp = now(); const previous = selected.level; setCases((current) => current.map((item) => item.id === selected.id ? { ...item, level: nextLevel, levelHistory: [...item.levelHistory, { at: timestamp, ...actor(), from: previous, to: nextLevel }], audit: [...item.audit, { at: timestamp, ...actor(), action: `Nivel cambiado N${previous} → N${nextLevel}` }] } : item)); };
  const submitResponse = () => { if (role !== 'physician' || !selected || !responseText.trim() || selected.status === 'CERRADO') return; const timestamp = now(); const response = { id: `${selected.id}-R${(selected.responses || []).length + 1}`, text: responseText.trim(), author: session.displayName, authorId: session.id, createdAt: timestamp, status: 'EMITIDA', statusHistory: [{ at: timestamp, from: 'BORRADOR', to: 'EMITIDA' }] }; patchCase(selected.id, { responses: [...(selected.responses || []), response], respondedBy: session.displayName, respondedById: session.id, respondedAt: timestamp, status: 'RESPONDIDO' }, 'Respuesta facultativa emitida'); setResponseText(''); };
  const advanceMessage = (target) => { const responses = selected?.responses || []; const last = responses[responses.length - 1]; if (!last || selected.status === 'CERRADO') return; const expected = last.status === 'EMITIDA' ? 'ENTREGADA' : last.status === 'ENTREGADA' ? 'LEIDA' : null; if (target !== expected) return; const timestamp = now(); const updated = { ...last, status: target, statusHistory: [...last.statusHistory, { at: timestamp, from: last.status, to: target, evidence: target === 'ENTREGADA' ? 'acuse técnico sintético' : 'acuse de lectura sintético' }] }; patchCase(selected.id, { responses: [...responses.slice(0, -1), updated] }, `Respuesta ${target.toLowerCase()} con evidencia sintética`); };
  const addAddendum = () => { if (!selected || !addendumText.trim() || role === 'admin') return; const timestamp = now(); setCases((current) => current.map((item) => item.id === selected.id ? { ...item, addenda: [...item.addenda, { at: timestamp, ...actor(), text: addendumText.trim() }], audit: [...item.audit, { at: timestamp, ...actor(), action: 'Adenda añadida; contenido previo conservado' }] } : item)); setAddendumText(''); };
  const closeReason = () => { if (!selected || selected.status !== 'RESPONDIDO') return 'El episodio todavía no está respondido.'; if (selected.followUpPending) return 'Hay seguimiento pendiente.'; const last = (selected.responses || []).slice(-1)[0]; if (!last) return 'No existe respuesta facultativa.'; if (selected.acknowledgementRequired && last.status !== 'LEIDA') return 'Este circuito requiere evidencia de lectura antes del cierre.'; return null; };
  const closeCase = () => { const reason = closeReason(); if (reason || !['nurse', 'physician'].includes(role)) return; const timestamp = now(); patchCase(selected.id, { status: 'CERRADO', closedBy: session.displayName, closedById: session.id, closedAt: timestamp }, 'Caso cerrado'); };
  const logout = () => { signOut(); navigate('/interno/acceso'); };
  const lastResponse = (selected?.responses || []).slice(-1)[0];
  const blockedClose = closeReason();

  return <main className="min-h-screen bg-slate-950 text-slate-100 py-10" data-testid="internal-clinical-prototype"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="rounded-2xl border border-amber-400/40 bg-amber-400/10 p-4 mb-6 flex gap-3"><AlertTriangle className="w-5 h-5 text-amber-300 shrink-0"/><div><p className="font-semibold text-amber-200">PROTOTIPO CLÍNICO · SOLO DATOS SINTÉTICOS</p><p className="text-sm text-amber-100/80">No autorizado para asistencia real ni datos de salud reales. Los gates del Frente B siguen abiertos.</p></div></div>
    <header className="flex flex-col lg:flex-row lg:items-end justify-between gap-5 mb-8"><div><div className="flex items-center gap-2 text-cyan-300 text-sm font-semibold mb-2"><ShieldCheck className="w-4 h-4"/>GASI · Entorno interno de prueba</div><h1 className="text-3xl md:text-4xl font-bold">Canal Enfermería ↔ Facultativo</h1><p className="text-slate-400 mt-2">El estado del episodio y el estado de entrega del mensaje son independientes.</p></div><div className="rounded-xl border border-slate-700 bg-slate-900 p-4 min-w-[310px]"><p className="text-xs uppercase text-slate-500">Identidad individual activa</p><p className="font-bold mt-1">{session.displayName}</p><p className="text-sm text-slate-400">{session.roleLabel} · {session.id}</p><button type="button" onClick={logout} className="mt-3 inline-flex items-center gap-2 text-sm rounded-lg border border-slate-700 px-3 py-2"><LogOut className="w-4 h-4"/>Cerrar sesión</button></div></header>
    <section className="grid md:grid-cols-3 gap-4 mb-6"><div className="rounded-xl bg-slate-900 border border-slate-800 p-4"><p className="text-slate-500 text-sm">Rol autorizado</p><p className="font-semibold flex items-center gap-2 mt-1"><RoleIcon className="w-5 h-5 text-cyan-300"/>{session.roleLabel}</p></div><div className="rounded-xl bg-slate-900 border border-slate-800 p-4"><p className="text-slate-500 text-sm">Pendientes visibles</p><p className="text-2xl font-bold mt-1">{pendingCount}</p></div><div className="rounded-xl bg-slate-900 border border-slate-800 p-4"><p className="text-slate-500 text-sm">Persistencia</p><p className="font-semibold mt-1 flex items-center gap-2"><LockKeyhole className="w-5 h-5 text-emerald-300"/>Solo memoria del prototipo</p></div></section>
    {role === 'nurse' && <button type="button" onClick={() => setShowCreate((v) => !v)} className="mb-5 inline-flex items-center gap-2 rounded-lg bg-cyan-400 text-slate-950 font-semibold px-4 py-2"><Plus className="w-4 h-4"/>Abrir caso sintético</button>}
    {showCreate && role === 'nurse' && <form onSubmit={createCase} className="rounded-2xl border border-slate-700 bg-slate-900 p-5 mb-6 grid md:grid-cols-2 gap-4"><input value={draft.patientRef} onChange={(e)=>setDraft({...draft,patientRef:e.target.value})} className="rounded-lg bg-slate-950 border border-slate-700 px-3 py-2"/><select value={draft.center} onChange={(e)=>setDraft({...draft,center:e.target.value})} className="rounded-lg bg-slate-950 border border-slate-700 px-3 py-2">{session.centers.map((c)=><option key={c}>{c}</option>)}</select><select value={draft.level} onChange={(e)=>setDraft({...draft,level:Number(e.target.value)})} className="rounded-lg bg-slate-950 border border-slate-700 px-3 py-2"><option value={1}>Nivel 1 · Urgencia</option><option value={2}>Nivel 2 · Consulta no aguda</option><option value={3}>Nivel 3 · Gestión</option></select><textarea required value={draft.summary} onChange={(e)=>setDraft({...draft,summary:e.target.value})} rows={3} className="md:col-span-2 rounded-lg bg-slate-950 border border-slate-700 px-3 py-2"/>{Number(draft.level)===1&&<div className="md:col-span-2 border border-red-400/40 bg-red-500/10 p-3 rounded-lg flex gap-2"><PhoneCall className="w-5 h-5"/><span><strong>Nivel 1:</strong> llamar directamente al facultativo; la web no sustituye ni retrasa la llamada.</span></div>}{Number(draft.level)===3&&<div className="md:col-span-2 border border-amber-400/40 bg-amber-400/10 p-3 rounded-lg">Indicación, prescripción y actuación derivada: <strong>BLOQUEADAS PARA ACTIVACIÓN REAL</strong>.</div>}<button type="submit" className="rounded-lg bg-cyan-400 text-slate-950 font-semibold px-4 py-2">Crear caso</button></form>}
    {role === 'admin' ? <section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-bold flex items-center gap-2"><UserCog className="w-5 h-5 text-cyan-300"/>Panel de coordinación</h2><p className="text-slate-400 mt-2">Solo metadatos operativos; esta vista no expone narrativa clínica.</p>{visibleCases.map((c)=><div key={c.id} className="mt-4 border-t border-slate-800 pt-3 text-sm">{c.id} · N{c.level} · {c.status} · {c.center}</div>)}</section> : <section className="grid lg:grid-cols-[340px_1fr] gap-6"><aside className="rounded-2xl border border-slate-800 bg-slate-900 p-4"><h2 className="font-bold mb-3">Casos</h2>{visibleCases.map((c)=><button type="button" key={c.id} onClick={()=>setSelectedId(c.id)} className={`w-full text-left rounded-lg border p-3 mb-2 ${selected?.id===c.id?'border-cyan-400':'border-slate-800'}`}><div className="flex justify-between gap-2"><span className="font-semibold">{c.id}</span><span className={`text-xs border rounded px-2 py-0.5 ${statusBadge(c.status)}`}>{c.status}</span></div><p className="text-xs text-slate-400 mt-1">N{c.level} · {c.center}</p></button>)}</aside><article className="rounded-2xl border border-slate-800 bg-slate-900 p-6">{selected&&<><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-bold">{selected.id}</h2><p className="text-sm text-slate-400">{selected.patientRef} · {LEVELS[selected.level].label}</p></div><span className={`h-fit text-xs border rounded px-2 py-1 ${statusBadge(selected.status)}`}>{selected.status}</span></div>{selected.level===1&&<div className="mt-4 border border-red-400/40 bg-red-500/10 p-3 rounded-lg flex gap-2"><PhoneCall className="w-5 h-5"/><span>Nivel 1: el canal telefónico directo es obligatorio; este registro no acredita por sí solo que la llamada se haya completado.</span></div>}<div className="mt-5 rounded-xl bg-slate-950 p-4"><p className="text-xs text-slate-500">Situación sintética</p><p className="mt-1">{selected.summary}</p></div>{role==='nurse'&&selected.status!=='CERRADO'&&<div className="mt-4 flex gap-2">{[1,2,3].map((n)=><button type="button" key={n} disabled={selected.level===n} onClick={()=>changeLevel(n)} className="border border-slate-700 rounded px-3 py-1 text-sm disabled:opacity-40">N{n}</button>)}</div>}{role==='physician'&&selected.status!=='CERRADO'&&<div className="mt-5"><textarea value={responseText} onChange={(e)=>setResponseText(e.target.value)} rows={3} placeholder="Respuesta facultativa sintética" className="w-full rounded-lg bg-slate-950 border border-slate-700 px-3 py-2"/><button type="button" onClick={submitResponse} className="mt-2 inline-flex items-center gap-2 rounded-lg bg-cyan-400 text-slate-950 font-semibold px-4 py-2"><Send className="w-4 h-4"/>Emitir respuesta</button></div>}{lastResponse&&<div className="mt-5 rounded-xl border border-slate-700 p-4"><div className="flex flex-wrap justify-between gap-2"><p className="font-semibold">Última respuesta facultativa</p><span className={`text-xs rounded px-2 py-1 ${messageBadge(lastResponse.status)}`}>{lastResponse.status}</span></div><p className="mt-2 text-slate-200">{lastResponse.text}</p><p className="text-xs text-slate-500 mt-2">Emitida por {lastResponse.author} · {lastResponse.createdAt}</p><p className="text-xs text-slate-400 mt-2">RESPONDIDO significa que existe respuesta registrada; no significa entregada, leída ni ejecutada.</p>{lastResponse.status==='EMITIDA'&&<button type="button" onClick={()=>advanceMessage('ENTREGADA')} className="mt-3 border border-cyan-700 rounded px-3 py-2 text-sm">Simular evidencia de entrega</button>}{lastResponse.status==='ENTREGADA'&&<button type="button" onClick={()=>advanceMessage('LEIDA')} className="mt-3 border border-emerald-700 rounded px-3 py-2 text-sm">Simular evidencia de lectura</button>}</div>}<div className="mt-5"><textarea value={addendumText} onChange={(e)=>setAddendumText(e.target.value)} rows={2} placeholder="Adenda sintética; no sustituye contenido previo" className="w-full rounded-lg bg-slate-950 border border-slate-700 px-3 py-2"/><button type="button" onClick={addAddendum} className="mt-2 border border-slate-700 rounded px-3 py-2 text-sm">Añadir adenda</button></div>{selected.addenda?.length>0&&<div className="mt-4"><p className="font-semibold flex items-center gap-2"><History className="w-4 h-4"/>Adendas</p>{selected.addenda.map((a,i)=><div key={`${a.at}-${i}`} className="text-sm border-l border-slate-700 pl-3 mt-2">{a.at} · {a.actor}: {a.text}</div>)}</div>}<div className="mt-6 border-t border-slate-800 pt-4"><div className="flex gap-2 items-start">{blockedClose?<AlertTriangle className="w-5 h-5 text-amber-300 shrink-0"/>:<CheckCircle2 className="w-5 h-5 text-emerald-300 shrink-0"/>}<div><p className="font-semibold">Cierre del episodio</p><p className="text-sm text-slate-400">{blockedClose||'Sin bloqueo técnico de cierre en esta simulación.'}</p></div></div><button type="button" disabled={Boolean(blockedClose)} onClick={closeCase} className="mt-3 rounded-lg bg-emerald-500 text-slate-950 font-semibold px-4 py-2 disabled:bg-slate-700 disabled:text-slate-400">Cerrar caso</button></div><div className="mt-6 border-t border-slate-800 pt-4"><p className="font-semibold flex items-center gap-2"><Clock3 className="w-4 h-4"/>Trazabilidad</p>{selected.audit.map((e,i)=><p key={`${e.at}-${i}`} className="text-xs text-slate-400 mt-2">{e.at} · {e.actor} ({e.role}) · {e.action}</p>)}</div></>}</article></section>}
  </div></main>;
}
